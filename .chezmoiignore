# Files to ignore when applying to home directory
CLAUDE.md
README.md

# macOS only applications
{{ if ne .chezmoi.os "darwin" }}
.config/karabiner
.config/vp
{{ end }}

# Skip secret files (Bitwarden-dependent files)
{{ if env "SKIP_SECRETS" }}
.ssh/config
.gitconfig
.gitconfig.work
.zshrc.local
{{ end }}
